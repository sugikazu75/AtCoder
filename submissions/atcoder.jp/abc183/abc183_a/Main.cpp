#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for(int i = 0; i < n; i++)
#define all(v) v.begin(), v.end()
typedef long long ll;

int main(){
    int x;
    cin >> x;
    if(x >= 0) cout << x << endl;
    else cout << 0 << endl;
}